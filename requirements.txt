# GenAI content-detection — pinned with floors.
# Task 1 (LogReg from scratch) needs only numpy/pandas; sklearn is for Tasks 2-3.

# --- core ---
numpy>=1.24
pandas>=2.0

# --- dimensionality reduction (Task 2: PCA) ---
scikit-learn>=1.3
# sparse CSR feature path + rank statistics; a sklearn dependency already, pinned
# explicitly because src/data.py and src/ensemble.py import it directly.
scipy>=1.10

# --- plotting / EDA ---
matplotlib>=3.7
seaborn>=0.12

# --- models / persistence (Task 3) ---
joblib>=1.3
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# --- notebooks ---
jupyter>=1.0

# --- reporting (Task 4 deliverables) ---
# Used only by reports/build_report.py and reports/build_deck.py, which generate the
# submitted report and slides from the result ledgers in data/processed/. Kept as a
# separate group because nothing in src/ or notebooks/ imports them.
python-docx>=1.1
python-pptx>=0.6
